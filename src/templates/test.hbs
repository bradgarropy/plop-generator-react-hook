import {renderHook} from "@testing-library/react-contexts"
import { {{camelCase name}} } from "hooks"

test("renders hook", () => {
    const {result} = renderHook(() => {{camelCase name}}())
    expect(result.current).toBeUndefined()
})
