import {createContext} from "react"

const {{pascalCase name}}Context = createContext()

const {{pascalCase name}}Provider = ({children}) => {
    const context = {}

    return (
        <{{pascalCase name}}Context.Provider value={context}>
            {children}
        </{{pascalCase name}}Context.Provider>
    )
}

export { {{pascalCase name}}Context, {{pascalCase name}}Provider}
